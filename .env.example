# ==============================================================================
# LOBBY CORE INFRASTRUCTURE CONFIGURATION
# ==============================================================================

# The shared Docker network name used for container-to-container communication.
# All apps wanting to be proxied by Traefik must join this network.
LOBBY_DOCKER_NETWORK=lobbby-network

# ==============================================================================
# PATH & DIRECTORY SETTINGS
# ==============================================================================

# Base directory for the project (Note: ${PWD} refers to the current working dir).
LOBBY_DIR=${PWD}

# Storage for container configurations.
LOBBY_CONFIG_DIR=${LOBBY_DIR}/configs

# Persistent data storage for applications (Databases, app state).
LOBBY_DATA_DIR=${LOBBY_DIR}/data

# Centralized logging directory for all containers in the stack.
LOBBY_LOG_DIR=${LOBBY_DIR}/logs

# ==============================================================================
# NETWORK & DOMAIN SETTINGS (Pure Off-The-Cloud)
# ==============================================================================

# The primary domain name for your local ecosystem (e.g., service.lobby.local).
LOBBY_TRAEFIK_DOMAIN=lobby

# The Top Level Domain (TLD). For local setups, .home or .lan are standard.
LOBBY_TRAEFIK_TLD=local
